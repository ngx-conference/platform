import { Routes } from '@angular/router'
import { NotFoundComponent } from './components/not-found/not-found.component'

export const AdminCoreRoutes: Routes = [
  {
    path: 'not-found',
    component: NotFoundComponent,
  },
]
